pluginManagement {
	repositories {
		maven {
			name = 'Fabric'
			url = 'https://maven.fabricmc.net/'
		}
		mavenCentral()
		gradlePluginPortal()
	}
}

rootProject.name = "chunkymap"
include("chunkymap-common", "chunkymap-spigot", "chunkymap-fabric")
